export default function ModalDefault() {
  // Default (empty) slot for the @modal parallel route.
  // Next.js requires a default component so the slot exists when no modal is active.
  return null
}
